# Review: stage2-004 registry base_url P0 修复（7 处确定性错误）

- 评审对象：commit `94b5000`（分支 task/stage2-004，HEAD）
- 改动文件：`aimux-providers/src/openai_compat_registry.rs`（1 文件，8 insertions / 7 deletions）
- 对照数据：`docs/plan/tasks/stage2-004.md` 修复清单 + `docs/internal/model-config-research/batch-02/03/05/06.md` 对应条目 + `_global_table.md` P0 节
- 核实日期：2026-08-02
- 说明：工作区存在无关未提交改动（aimux-core/bindings/*.ts、provider-inventory/*、scripts/* 等，与本任务无关），本次仅写 review 文件，未 stage/提交任何内容。

## 核对项

### 1. diff 范围：仅 7 处 base_url 字符串 + freemodel 注释 ✅
`git show 94b5000` 逐一检查：7 处修改全部为 `declare_openai_compat_provider!` 宏调用中的 base_url 字面量（第 4 个参数），外加 freemodel 条目上方 1 条注释；**display 名、env_var、profile（`OpenAICompatProfile::full()`）均未改动**，宏定义本身与类型声明（XxxConfig/XxxProvider）未触碰。无任何其他字段/文件改动，无越界（存疑 ~20 家未动）。

### 2. 每处修正值与任务表 / batch 证据一致 ✅

| 行(改后) | 厂商 | 修正值 | 证据 |
|---|---|---|---|
| 759 | firepass | `https://api.fireworks.ai/inference/v1` | batch-02.md:52（fireworks 条目同端点）+ 1422-1425（同一厂商同一端点，乱码截断 `锛圤penAI`） |
| 778 | freemodel | `https://api.freemodel.dev/v1` ⚠️ | batch-03.md:82 建议值「如 `https://api.freemodel.dev/v1` 之类」（C 级官网，base_url 未确认） |
| 841 | gmi | `https://api.gmi-serving.com/v1` | batch-03.md:19（gmicloud 同 URL）+ 284（乱码截断 `（与`） |
| 940 | iflowcn | `https://apis.iflow.cn/v1` | batch-03.md:29, 636（乱码截断 `（chat`） |
| 1534 | opencode | `https://api.opencode.zen/v1` | batch-05.md:13（opencode_zen 变体同 URL，B 级 providers.mdx）+ 58-72（原值 `"opencode_zen.rs"` 非法） |
| 2128 | volc_engine | `https://ark.cn-beijing.volces.com/api/v3` | batch-06.md:840（B 级 simple-one-api/uni-api）+ 848 建议动作「改 `/api/v3`」 |
| 2254 | zhipu_v4 | `https://open.bigmodel.cn/api/paas/v4` | batch-06.md:1288（C 级 docs.bigmodel.cn）+ 1296 建议动作「改 `/api/paas/v4`」 |

任务表所列改前行号（1533/759/777/939/840/2127/2253）与 diff hunk 上下文逐一吻合；`_global_table.md` P0 节 7 行（L27-33）与任务表一致。

### 3. freemodel 推断值已标注 ⚠️ ✅
[openai_compat_registry.rs:777](D:\code\aimux\aimux-providers\src\openai_compat_registry.rs#L777) 条目行内注释 `// ⚠️ freemodel base_url 为推断值，待实测确认 (batch-03)`，符合任务约束 #3（条目行内或模块注释标注）。

### 4. 公共类型签名不变 ✅
diff 只改宏调用实参字符串；`cargo check -p aimux-providers` 通过。

### 5. 构建与测试
- `cargo check -p aimux-providers`：通过
- `cargo test -p aimux-providers --tests`：全部 suite ok，0 failed（无断言旧 base_url 的测试；conformance 用 mock server 不受影响）
- 工作树 `openai_compat_registry.rs` 与 commit 94b5000 一致（无后续漂移）；94b5000 为 task/stage2-004 HEAD

## Findings

### F1. freemodel base_url 仍为推断值，需实测兜底（P2，non-blocking）
- **位置**：openai_compat_registry.rs:778
- **证据**：batch-03.md:72、88 —— 「正确 base_url 未确认」，存疑标记 ⚠️；本任务表亦将其列为推断值
- **判定**：修复值 `https://api.freemodel.dev/v1` 与 batch-03 建议一致且已按要求标注 ⚠️，本任务范围内合规；但该端点在发版前未经真实调用验证（batch-03 验证状态 🔲），存在请求失败风险 → 建议在发版前对该家做一次实测（或并入「存疑 20 家实测」清单），非阻塞。

### F2. firepass 与 fireworks 条目 display 名/端点重复未处理（P3，non-blocking）
- **证据**：batch-02.md:1425（「本条目为重复声明，建议合并」）；修复后 base_url 已一致，但两条目仍并存
- **判定**：合并去重属另一任务范围（本任务只修 base_url 确定性错误），本次不改正确；留待后续去重任务处理。

## 结论

**pass** —— 无 blocking finding。4 项核对全部通过：diff 恰为 7 处 base_url 字符串 + freemodel ⚠️ 注释，无 display/env_var/profile 或其他字段改动；7 处修正值与任务表及 batch-02/03/05/06 证据逐项一致；freemodel 推断值已按约束标注；公共类型签名不变。`cargo check` 与 `cargo test -p aimux-providers --tests` 全绿。F1（freemodel 待实测）、F2（firepass/fireworks 去重）为 non-blocking，建议并入后续实测/去重任务跟踪。
