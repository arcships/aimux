# Review: stage2-001 退役 RequestBodyOverride + max_tokens_key + warning

- 评审对象：commit `a733ce4`（分支 `task/stage2-001`，5 文件：`aimux-providers/src/openai/mod.rs`、`convert.rs`、`tests/deepseek_reasoning_test.rs`、`deepseek_chat_test.rs`、`body_overrides_test.rs`）
- 设计依据：`docs/plan/analysis/stage2-reasoning-map.md`（v3）§0-§2、§4 I1-I3；`rfc/0017-provider-config-dx.md` §2.3/阶段 2
- 验证手段：`cargo check -p aimux-providers` ✓；`cargo test -p aimux-providers --lib` ✓（18 通过）；`cargo test -p aimux-providers --test body_overrides_test --test deepseek_reasoning_test --test deepseek_chat_test` ✓（14 + 19 + 20 = 53 通过）；全仓库 grep 残留
- 工作区无关未提交改动（provider-inventory、docs/internal/gateway-research、reference-audit、scripts 等）经 `git status` 确认为非本任务产出，未纳入评审。

## Findings

### F1. 退役完整性 —— 无残留（P3，non-blocking）
- **设计位置**：stage2-reasoning-map §0 / §2.1（删 `RequestBodyOverride`/`request_body_override`/`apply_deepseek_override`/归一化，`deepseek()` 回归 `full()`）
- **代码位置**：[mod.rs](D:\code\aimux\aimux-providers\src\openai\mod.rs#L45)（字段替换、`deepseek()` = `Self::full()` 于 L79-81）；[convert.rs](D:\code\aimux\aimux-providers\src\openai\convert.rs#L1446)（后处理调用点替换为退役注释；`apply_deepseek_override` 整段删除）
- **核验**：grep 全仓库 `RequestBodyOverride|request_body_override|apply_deepseek_override` 无 src 命中，仅 docs 与 [deepseek_chat_test.rs](D:\code\aimux\aimux-providers\tests\deepseek_chat_test.rs#L354) 一处说明性注释提及名称；`deepseek()` 回归 `full()` 后能力位（top_k/tools/response_format/stream_usage_key）与旧特化全等，无能力回归。registry `deepseek` 行仍调 `deepseek()`，结构不变（§2.1「薄封装保留」）。

### F2. max_tokens_key 三值域分支语义正确（P3，non-blocking）
- **设计位置**：stage2-reasoning-map §2.3；任务 objective 第 2 条
- **代码位置**：[convert.rs](D:\code\aimux\aimux-providers\src\openai\convert.rs#L1128)（`use_mct_key` 分支）+ L1142-1147（mct 选项守卫）
- **核验**（对照验收点逐一成立）：
  - `Some("max_tokens")` + 推理模型 → 只发 `max_tokens`、不含 `max_completion_tokens` ✓（测试 `max_tokens_key_max_tokens_reasoning_model`）
  - `Some("max_completion_tokens")` → 非推理分支也发 mct ✓（测试 `max_tokens_key_max_completion_tokens_non_reasoning`）
  - `None` → `is_reasoning_model` 推断（推理 mct / 非推理 max_tokens），与退役前默认行为逐例等价（含「非推理同时发 max_tokens+mct」「推理时 mct 选项优先」两条旧语义），回归安全 ✓
  - 未知值兜底 `_ => is_reasoning_model`，安全 ✓

### F3. warning 两路径正确（P3，non-blocking）
- **设计位置**：stage2-reasoning-map §2.4 + §4 I1/I2（未发 effort → warning「未翻译,该厂商需 bodyOverrides」；已发 effort → 不 warning 防误报）
- **代码位置**：[convert.rs](D:\code\aimux\aimux-providers\src\openai\convert.rs#L1340)（`is_custom_reasoning && resolved_reasoning_effort.is_none()` → `Warning::Compatibility{feature:"reasoning", …}`，文案含设计指定串）
- **核验**：
  - 未发 effort：groq 跳过 `'none'` → warning 透出 ✓（测试 `warning_when_reasoning_not_translated`，且断言请求体无 `reasoning_effort`）
  - 已发 effort：非 groq `'none'` 透传为 `"none"` → 无 warning ✓（测试 `no_warning_when_reasoning_translated`，断言请求体含 `reasoning_effort:"none"`）
  - 语义自洽性：非 groq 厂商 7 档全部 resolve（含 none），groq 的 minimal/xhigh 有映射也 resolve，故 warning 当前实际仅在 groq+`'none'` 触发——与设计「无任何映射且未发 effort」等价，无过报/漏报路径发现。

### F4. 用户 API 零变化（P3，non-blocking）
- **设计位置**：stage2-reasoning-map §2.5（兼容性承诺）
- **代码位置**：[mod.rs](D:\code\aimux\aimux-providers\src\openai\mod.rs#L49)
- **核验**：`lib.rs` 无新导出（`git diff` 未触碰）；无新增用户参数/选项/绑定层改动。唯一新增公开面是 `pub max_tokens_key` 字段——设计 §2.3 明文声明 `pub`，且是替换被删的 `request_body_override`（同为内部管线字段，非用户概念），加法不破坏。删除 `pub RequestBodyOverride` 枚举属设计退役目标（内部管线类型），不构成用户 API 违约。

### F5. 测试改造符合新语义（P3，non-blocking）
- **设计位置**：stage2-reasoning-map §3 M2（测试）/ §5 验收；任务 verification 2-3
- **代码位置**：[deepseek_reasoning_test.rs](D:\code\aimux\aimux-providers\tests\deepseek_reasoning_test.rs)（10 个特化断言全部改写）、[deepseek_chat_test.rs](D:\code\aimux\aimux-providers\tests\deepseek_chat_test.rs#L351)（tool-call 测试）、[body_overrides_test.rs](D:\code\aimux\aimux-providers\tests\body_overrides_test.rs#L175)（4 个新验收测试）
- **核验**：
  - deepseek 旧测试由「断言 thinking 注入/归一化」改为「断言 thinking 缺席 + effort 直传 + 无归一化 warning」，与退役语义一致；透传断言合理（`'none'`→`"none"`、`'xhigh'`→`"xhigh"`、`'minimal'`→`"minimal"`，均直传无翻译）；`providerOptions.deepseek.*` 被忽略的断言与共享 builder 只读 `openai`/`groq` key 的行为一致 ✓
  - 4 个新验收测试覆盖任务 verification 第 2 条全部列项：none 透传/xhigh 直传（deepseek_reasoning_test）+ warning 两路径 + max_tokens_key 两分支（body_overrides_test）✓
  - 备注：`max_tokens_key=None` 兜底分支与 `Some("max_tokens")` 非推理分支无显式新测试（由既有测试覆盖默认路径），属可接受缺口；任务把测试改造名义上划给 stage2-002，本提交提前完成并全绿，优于任务下限。

### F6. groq minimal→low / xhigh→high 映射保留（P2，non-blocking）
- **设计位置**：stage2-reasoning-map §0「退役的机制：reasoning_effort 归一化……改为直接透传」、§1「不做：任何内置厂商映射（枚举/数据均不保留）」
- **代码位置**：[convert.rs](D:\code\aimux\aimux-providers\src\openai\convert.rs#L1048)（共享 builder 内 groq 分支，`Minimal→low`、`Xhigh→high`、`None→跳过`）
- **核验**：该映射位于共享 builder 的 groq 条件分支（非 `apply_deepseek_override`），为本提交前已存在、提交未触碰的代码。与设计 v3「不保留任何内置厂商映射」在文本上冲突（同为 effort 档位归一化的机制类别），但**不在本任务 objective 范围内**（objective 第 1 条的归一化退役明确指向 `apply_deepseek_override` 路径；任务 path 段仅要求 registry 行「若引用特化则同步」，groq() 行未引用特化），也不违反任何验收点；warning 语义不受影响（有映射→已 resolve→不 warning，与 §2.4 自洽）。
- **判定**：设计原则文本冲突、任务边界内无违约 → **non-blocking**。需后续任务显式处置：删除映射令 groq 全透传（行为变化需测试与手册同步），或由设计文档明确保留 groq 特例并写入用户手册。

### F7. 厂商级 max_tokens_key 数据接线未做（P2，non-blocking）
- **设计位置**：stage2-reasoning-map §2.3 内置行表（stepfun/siliconflow/sarvam/reka/publicai/perplexity → `"max_tokens"`；groq/heroku → `"max_completion_tokens"`）、§2.5「修 bug（groq/heroku 行为变化属修复）」
- **代码位置**：[openai_compat_registry.rs](D:\code\aimux\aimux-providers\src\openai_compat_registry.rs)（全部行仍 `full()`/`groq()`，`max_tokens_key` 无任何赋值）
- **核验**：任务 objective 第 2 条只要求「内部字段 + 分支改造」——**不违反任务边界**（字段+分支已实现并测试）。但注意：接线未做意味着设计 §2.5 承诺的 groq/heroku 真实修复（非推理模型不再发已弃用的 `max_tokens`）与 6 家「只认 max_tokens」厂商的保护尚未对真实流量生效——本任务交付后效果仅存在于构造 profile 的测试层面。
- **判定**：**non-blocking**（任务边界内合规），但必须在阶段 2 收尾（stage2-002/003 或新任务）前完成接线，否则该修复对真实厂商仍为 dead code。建议在 stage2-002/003 的验收矩阵中锁定 groq/heroku 请求体断言。

### F8. `Some("max_tokens")` + 显式 maxCompletionTokens 选项被静默丢弃（P3，non-blocking）
- **设计位置**：stage2-reasoning-map §2.3（「只认 max_tokens」「只发 max_tokens」）；设计未规定该丢弃路径的提示行为
- **代码位置**：[convert.rs](D:\code\aimux\aimux-providers\src\openai\convert.rs#L1142)
- **核验**：方向与设计一致（不向只认 max_tokens 的厂商发送 mct），但用户显式传入的 `maxCompletionTokens` 会被无警告丢弃，且行为相对退役前（非推理分支会发 mct）有变化，设计文档未提及该细节。实现合理、非违约 → non-blocking；建议用户手册标注或在后续任务加 `Warning::Unsupported`。

## 结论

**pass** —— 无 blocking finding，无对接路径 stub/mock/fake 残留，与设计文档 contract 一致。

- 退役（F1）、max_tokens_key 三值域分支（F2）、warning 两路径（F3）、用户 API 零变化（F4）、测试改造（F5）全部核验通过，`cargo check` + 全部相关测试 53 项全绿。
- 两个 P2 均为任务边界外事项：F6（groq 映射保留，与设计 v3 原则文本冲突但不在 objective 内）、F7（厂商级接线未做，objective 只要求字段+分支）。二者均不阻塞本任务，但须在阶段 2 收尾前处置（F6 需设计决策：删除或显式保留；F7 需接线并补真实厂商断言）。
