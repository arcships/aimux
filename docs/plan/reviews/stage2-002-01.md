# Review: stage2-002 测试套件（退役回归 + max_tokens_key 矩阵 + 厂商数据接线）

- 评审对象：commit `b24e59e`（分支 `task/stage2-002`，3 文件：`aimux-providers/src/openai/mod.rs`、`openai_compat_registry.rs`、`tests/reasoning_map_test.rs`）
- 设计依据：`docs/plan/analysis/stage2-reasoning-map.md`（v3）§2.3、§4 I4-I5、§5；`rfc/0017-provider-config-dx.md` §2.3/阶段 2；`docs/plan/backlog.md` B9；`rfc/0012-source-dedup.md` §3.1（公共签名不变约束）
- 验证手段：
  - `cargo test -p aimux-providers --test reasoning_map_test` ✓（10/10 通过）
  - `cargo test -p aimux-providers --tests` ✓（116 个 test binary 全部 ok，含 deepseek_chat/deepseek_reasoning/openai_model/body_overrides 等）
  - grep 全仓库 `RequestBodyOverride|apply_deepseek_override|request_body_override`：src 无残留 ✓
  - 接线值与 `_global_table.md` §C（batch-03/batch-05 证据）逐项比对 ✓
- 工作区无关未提交改动（provider-inventory/、scripts/、docs/ 部分）经 `git status` 确认为非本任务产出，未纳入评审。未提交任何文件。

## Findings

### F1. B9 厂商级 max_tokens_key 接线 —— 8 家全部与设计 §2.3 一致（P3，non-blocking）
- **设计位置**：stage2-reasoning-map §2.3 内置行表；backlog B9（「须在 stage2-002/003 收尾前接线，否则 groq/heroku 修复对真实流量不生效」）
- **代码位置**：[mod.rs](D:\code\aimux\aimux-providers\src\openai\mod.rs#L67)（`groq()` → `max_tokens_key: Some("max_completion_tokens")`）；[openai_compat_registry.rs](D:\code\aimux\aimux-providers\src\openai_compat_registry.rs#L893)（heroku）、#L1595（perplexity）、#L1694（publicai）、#L1739（reka_ai）、#L1793（sarvam）、#L1820（siliconflow）、#L1856（stepfun）
- **核验**：逐行比对设计 §2.3 与 `_global_table.md` §C（batch-03：groq max_tokens 弃用 / heroku 只认 mct；batch-05：6 家只认 max_tokens）——stepfun/siliconflow/sarvam/reka_ai/publicai/perplexity → `"max_tokens"`（6 家）✓，groq/heroku → `"max_completion_tokens"`（2 家）✓，与设计表 8 行完全一致，无多接/漏接/错接。stage2-001 评审 F7 的收尾义务已闭环。

### F2. RFC-0012 公共签名约束 —— XxxConfig/XxxProvider 零改动（P3，non-blocking）
- **设计位置**：rfc/0012-source-dedup.md §3.1「public API unchanged——externally exported `XxxConfig`/`XxxProvider` type names and constructors remain unchanged」
- **代码位置**：[openai_compat.rs](D:\code\aimux\aimux-providers\src\openai_compat.rs#L39)（宏展开 `XxxConfig`/`XxxProvider` 结构体与构造器）；[mod.rs](D:\code\aimux\aimux-providers\src\openai\mod.rs#L79)（新增 `with_max_tokens_key` builder）
- **核验**：registry 行仅 `$profile` 表达式变化（`full()` → `full().with_max_tokens_key(..)`；groq 行本就用 `groq()`），config/provider 类型名与构造器零变化；`with_max_tokens_key` 为 `OpenAICompatProfile` 新增方法（纯加法，不改变既有类型签名）。`git diff` 确认无 `lib.rs`/绑定层/其他公共面改动。

### F3. 测试覆盖 —— 任务 verification 全部列项闭环（P3，non-blocking）
- **设计位置**：stage2-reasoning-map §4 I4-I5、§5 验收；任务 verification 1-4
- **代码位置**：[reasoning_map_test.rs](D:\code\aimux\aimux-providers\tests\reasoning_map_test.rs)（10 个测试）
- **核验**（逐列项）：
  - I4 退役回归：`i4_deepseek_retired_no_thinking_injection`（`reasoning:'none'` → `reasoning_effort:"none"` + 无 `thinking`）+ `i4_deepseek_no_thinking_when_reasoning_unset` ✓
  - I5 阶段 1 能力回归：`i5_body_overrides_injects_thinking_disabled/enabled`（per-call `body_overrides` 原样进请求体，与 `deep_merge_json` 路径 [convert.rs](D:\code\aimux\aimux-providers\src\openai\convert.rs#L1437) 一致）✓
  - max_tokens_key 矩阵 × 推理/非推理两分支：`max_tokens_key_matrix_reasoning_branch`（o3-mini）+ `max_tokens_key_matrix_non_reasoning_branch`（gpt-4o），8 家全遍历，且断言「期望 key 在、另一 key 缺席」✓
  - 接线本身锁定（防死代码）：`max_tokens_key_wiring_registry` 直接断言各 `XxxConfig::new(..).profile.max_tokens_key` ✓（对应任务第 4 条「防死代码对照」）
  - 无 warning 断言：`no_reasoning_warning_on_direct_passthrough`（6 档自定义值均断言无 `Warning::Compatibility{feature:"reasoning"}`——直传语义下旧「未翻译」warning 块已删（[convert.rs](D:\code\aimux\aimux-providers\src\openai\convert.rs#L1331) 注释确认不可达），该断言防未来误加）✓
  - reasoning_effort 7 档直传：`reasoning_effort_passthrough_all_seven_levels`（ProviderDefault 不发字段 + none/minimal/low/medium/high/xhigh 原样透传，无 xhigh→max/minimal→low 归一化）✓
  - 对照表完整：`wired_vendors()` 清单与设计 §2.3 表 8 行逐项一致 ✓（任务 verification 第 4 条）
  - `max_tokens_key_none_keeps_inference`：未接线厂商（`full()`）保持现状推断（推理 mct / 非推理 max_tokens），回归护栏 ✓
  - 分支鉴别力分析：推理分支对 max_tokens 厂商有鉴别力（o3-mini + None 会推断发 mct → 断言失败即捕获误接线），非推理分支对 mct 厂商有鉴别力（gpt-4o + None 会发 max_tokens → 捕获 groq/heroku 误接线）；两分支互补，8 家接线整体闭环，无测试盲点。

### F4. DeepSeek 退役语义 —— 现有测试改造无遗漏（P3，non-blocking）
- **设计位置**：stage2-reasoning-map §2.1/§4 I4；任务 objective 第 1 条
- **代码位置**：[deepseek_reasoning_test.rs](D:\code\aimux\aimux-providers\tests\deepseek_reasoning_test.rs#L395)（特化断言全部按新语义改写：`should_not_inject_thinking_from_deepseek_provider_options`、`should_passthrough_reasoning_high/none`、`should_passthrough_reasoning_xhigh_without_normalization`）；[deepseek_chat_test.rs](D:\code\aimux\aimux-providers\tests\deepseek_chat_test.rs#L342)
- **核验**：
  - grep src 全量 `RequestBodyOverride|apply_deepseek_override|request_body_override` **零命中**，仅 [deepseek_chat_test.rs](D:\code\aimux\aimux-providers\tests\deepseek_chat_test.rs#L354) 一处说明性注释提及旧函数名——无死代码残留 ✓
  - 现有测试改造发生于 stage2-001 提交（a733ce4/4745542/c2b735c），非本 commit 内；本任务「现有 deepseek 测试改造」的**交付状态**已达成（全部按透传语义断言），b24e59e 以新测试文件补齐 I4/I5/矩阵/warning/7 档覆盖，任务 objective 整体闭环
  - 任务 path 提到的 `deepseek_test.rs`/`openai_model_test.rs` 实际不存在；对应文件为 `deepseek_chat_test.rs` + `deepseek_reasoning_test.rs`（均已改造），`openai_model_test.rs` 无 deepseek 用例（grep 零命中）——文档文件名过时，非实现缺陷
  - `OpenAICompatProfile::deepseek()` = `Self::full()`（[mod.rs](D:\code\aimux\aimux-providers\src\openai\mod.rs#L87)），§2.1「薄封装保留」✓

### F5. 测试方式 —— 直连 body 构造满足「config→profile→body 全链覆盖」说法（P3，non-blocking，说明性）
- **设计位置**：stage2-reasoning-map §4 标注 wiremock 断言；任务 verification 第 1-3 条
- **代码位置**：[reasoning_map_test.rs](D:\code\aimux\aimux-providers\tests\reasoning_map_test.rs#L148)（`wired_vendors()` 从 `XxxConfig::new(..)` 实际构造取 profile）、#L181（`build_request_body_with_warnings` 直连断言）
- **核验**：
  - 链路三段：**config→profile** 由 `max_tokens_key_wiring_registry` 用宏生成的真实 `XxxConfig::new()` 提取 `.profile` 锁定（取出的值即 `with_profile($profile)` 所存，无等价性缺口）；**profile→body** 由两分支矩阵断言锁定；**config→body** 组装成立
  - 未走 wiremock HTTP 往返的段是「body→wire 传输」——该段与 `max_tokens_key`（纯 body 构造关注点）正交，不影响接线正确性判定；且退役语义（I4/I5/透传）已有 [deepseek_reasoning_test.rs](D:\code\aimux\aimux-providers\tests\deepseek_reasoning_test.rs) wiremock 端到端覆盖（`request_body` 实测断言），矩阵直连测试是对 wiremock 套件的补充而非替代
  - 判定：**「接线链路 config→profile→body 全链覆盖」的说法成立**，无「接线只到 profile 未到 body」的缺口

### F6. stepfun_ai_step_plan / stepfun_step_plan 两行未接线（P3，non-blocking，观察）
- **设计位置**：stage2-reasoning-map §2.3（内置行仅列 stepfun 主行 8 家）；`_global_table.md` §C（「只认 max_tokens：stepfun…」按厂商族表述）
- **代码位置**：[openai_compat_registry.rs](D:\code\aimux\aimux-providers\src\openai_compat_registry.rs#L1861)（stepfun_ai_step_plan）、#L1870（stepfun_step_plan，均仍 `full()`）
- **核验**：两行为独立注册条目（`/step_plan/v1` 端点、独立 Config/Provider），设计表与任务对照表均只列 8 家，**不违反本任务 contract**。若调研证据按厂商族覆盖 step_plan 端点，可在 stage2-003 用户手册阶段一并确认是否同规则接线。

### F7. cerebras 未接线（P3，non-blocking，观察）
- **设计位置**：`_global_table.md` §C（「只认 max_completion_tokens：Heroku…、cerebras ✅、Groq」）；stage2-reasoning-map §2.3（8 家表不含 cerebras）
- **代码位置**：[openai_compat_registry.rs](D:\code\aimux\aimux-providers\src\openai_compat_registry.rs)（cerebras 行仍 `full()`）
- **核验**：调研表将 cerebras 标为只认 mct 厂商，设计 §2.3 未将其列入接线行——属设计范围取舍（cerebras `None` 时推理模型已推断发 mct，非推理模型发 max_tokens 或受影响）。非本任务违约；建议 B9 后续或调研复核时确认 cerebras 非推理模型真实行为。

## 结论

**pass** —— 无 blocking finding，无对接路径 stub/mock/fake 残留，与设计文档 contract 一致。

- F1 B9 接线 8 家与设计 §2.3 表逐项一致（stage2-001 评审 F7 收尾闭环）；F2 RFC-0012 公共签名零变化；F3 测试覆盖任务 verification 全部列项（I4/I5/矩阵×两分支/7 档直传/无 warning/对照表完整），两分支鉴别力互补无盲点；F4 退役无 src 残留、现有测试已按新语义改造；F5 直连 body 断言方式满足「config→profile→body 全链覆盖」说法，wiremock 端到端已由既有 deepseek 套件承担；F6/F7 均为设计范围外观察项，non-blocking。
- `cargo test -p aimux-providers --test reasoning_map_test`（10/10）与 `cargo test -p aimux-providers --tests`（116 个 binary 全 ok）全绿。
- 遗留（均不阻塞）：F6/F7 两个接线范围观察项建议在 stage2-003 手册/调研复核阶段确认；任务 path 文档文件名（deepseek_test.rs/openai_model_test.rs）过时，建议任务文件同步实际文件名。
