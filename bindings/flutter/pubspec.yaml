name: aimux
description: Unified LLM service layer for Flutter/Dart (Rust core, 290+ providers, C ABI via dart:ffi)
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  ffi: ^2.1.0
  json_annotation: ^4.9.0

dev_dependencies:
  test: ^1.25.0
  json_serializable: ^6.8.0
  build_runner: ^2.4.0
