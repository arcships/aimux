name: aimux
description: Unified LLM service layer for Flutter/Dart (Rust core, 172+ providers, C ABI via dart:ffi)
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  ffi: ^2.1.0

dev_dependencies:
  test: ^1.25.0
